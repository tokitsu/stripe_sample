var stripe = Stripe("<%= Rails.application.credentials[:stripe][:pubric] %>");

stripe.redirectToCheckout({
  sessionId: '<%= @session.id %>'
}).then(function (result) {
  console.log(result.error.message);
});
